This repo is product #69 of Febin's 50-SaaS challenge. Nothing is built yet — docs/ is
the source of truth. Read docs/LLD.md then docs/PLAN.md and execute tasks in order with
TDD. Stack conventions + the shipped reference implementation live in the private repo
febufenn-cyber/50-saas (contract-reviewer/) — same Worker+Supabase+credits+provider
patterns. Model strings, the default-deny-RLS-plus-token-gate design for the public
intake route, and the turn-cap/rate-limit cost guard in the LLD are verified decisions —
do not re-litigate without evidence.
